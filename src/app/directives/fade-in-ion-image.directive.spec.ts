import {FadeInIonImageDirective} from './fade-in-ion-image.directive';

describe('FadeInImageDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInIonImageDirective();
    expect(directive).toBeTruthy();
  });
});
